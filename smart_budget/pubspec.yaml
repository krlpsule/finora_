name: finora
description: Finora - Personal Finance Management
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management & Architecture
  flutter_bloc: ^8.1.3 
  equatable: ^2.0.5
  provider: ^6.0.5

  # AI & Core Services
  google_generative_ai: ^0.4.4
  flutter_dotenv: ^5.0.2
  http: ^1.1.0 # HTTP (for AI requests or general use)

  # Firebase & Database
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.0
  firebase_database: ^11.0.4

  # UI & Visualizations
  fl_chart: ^0.68.0

  # Speech & Notifications
  speech_to_text: ^7.3.0
  flutter_local_notifications: ^17.0.0 
  timezone: ^0.9.2 
  shared_preferences: ^2.2.2 

  # Utilities & Data
  intl: ^0.19.0 # Tekrarlanan intl paketi düzeltildi
  uuid: ^3.0.7
  
  # Data Parsing (CSV/Excel/PDF)
  file_picker: ^8.0.0
  csv: ^6.0.0
  excel: ^4.0.0
  syncfusion_flutter_pdf: ^24.1.41 # PDF 'pdf_text' 

dev_dependencies:
  flutter_test:
    sdk: flutter
  # flutter_lints: ^3.0.0 # Opsiyonel: Linter paketini ekleyin

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/finora_logo.jpeg